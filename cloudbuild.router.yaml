steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "gcr.io/$PROJECT_ID/router",
        "-f",
        "Dockerfile.router",
        ".",
      ]
images:
  - "gcr.io/$PROJECT_ID/router"
